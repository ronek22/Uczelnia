#include <stdio.h>
#include <string.h>


int main(){

	int i;
	char znaki[100],temp;
	printf("Wprowadz wyraz: ");
	scanf("%99s", znaki);
	int * last=strlen(znaki)-1;

	for(i=0;i<=length;i++){
		temp = *(znaki+i)
		znaki[i]=*(znaki+(length-i))
		znaki[length-i]=temp;
	}

	for(i=0;i<length;i++){
		printf("%c", znaki[i]);
	}

	printf("\n");
	return 0;
}
